# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data (too large for git)
*.csv
*.json
!configs/*.yaml
!figures/*.json
*.npy
*.npz
*.pt
*.pth
*.bin
*.safetensors
*.index
*.pkl

# Images
*.png
*.jpg
*.jpeg
*.tiff
*.dcm

# PDFs (generated figures)
figures/*.pdf

# Temp
/tmp/
*.log

# Secrets
.claude-secrets/
*.key

# Modal
.modal/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Large data directories
# NOTE: `data/` and `indices/` are no longer blanket-ignored. The
# 2026-04-14 v3 sprint audit found that `data/augmentation/*.py` and
# `data/preprocessing/*.py` — ~3000 lines of Python source used by
# Task 2, Task 3, and the existing preprocessing pipeline — had
# been silently excluded from git history since project inception
# because of the blanket `data/` rule. Actual binary / large data
# artifacts are already caught by the file-extension patterns above
# (*.csv, *.json, *.npy, *.pt, *.pth, *.bin, *.safetensors, *.pkl,
# *.png, *.jpg, *.dcm, *.ipynb) so removing the directory rules is
# safe and lets Python source under these paths flow into git.
checkpoints/

# Node
node_modules/

# R
.Rhistory

# LaTeX build artifacts (keep .tex/.sty/.bst/.bib sources)
*.aux
*.bbl
*.blg
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz

# Office binaries / lock files (derived from the markdown sources)
*.docx
~$*

# Internal working notes (handoffs, wakeups, status, execution logs, plans)
HANDOFF_*.md
WAKEUP_*.md
*_STATUS.md
*_STATUS_*.md
*_EXECUTION_LOG.md
*_RESUME_PROMPT.md
*_RUNBOOK.md
*_RUNBOOK_*.md
RUNBOOK_*.md
PIVOT_*_PLAN.md
PIVOT_PLAN_*.md
PIVOT_D_*.md
PLAN_V6_*.md
progress.md
